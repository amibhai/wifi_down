# WiFi Auditor — Python dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: The core aircrack-ng suite must be installed via the system
# package manager (see install.sh). These are pure-Python extras.

colorama>=0.4.6       # cross-platform ANSI colour support (optional fallback)
tabulate>=0.9.0       # pretty-print network tables
tqdm>=4.66.0          # progress bars during cracking / wordlist gen
